# Cine Studio Local MVP
# The MVP uses mock providers by default. Keep real keys server-side when live adapters are added.
VITE_CINE_STUDIO_PROVIDER=mock
VITE_CINE_STUDIO_SUPABASE_URL=
VITE_CINE_STUDIO_STRIPE_PUBLISHABLE_KEY=
